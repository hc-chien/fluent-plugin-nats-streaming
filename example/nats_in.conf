<system>
  log_level trace
  workers 5
</system>

<source>
  @type nats-streaming
  cluster_id test-cluster

  # support multi workers
  client_id "in-#{Socket.gethostname}-#{worker_id}"
  channel   nats.test
  queue     test
</source>

<match **>
  @type stdout
</match>
